language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t tomaszjudym/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tomaszjudym/docker-react npm run test
